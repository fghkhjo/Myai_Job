---
name: job-matcher
description: 通用岗位筛选与匹配技能（不限岗位类型）。用户只需提供 目标城市（一个或多个）+ 目标岗位名称 + 求职者简历，即可从权威招聘网站（BOSS直聘/智联招聘/前程无忧/猎聘/国聘/高校就业网等）检索真实岗位，按简历计算匹配度，同时输出三份交付文档——HTML 岗位推荐表格（先按城市排序、再按匹配度降序）、Word 文档（docx）、PDF 文档（pdf）。用户说出"找工作""筛选岗位""岗位匹配""求职"等关键词时触发
---

# 通用岗位筛选与匹配 Skill

## 技能定位
- **适用对象**：任意岗位（技术/产品/运营/设计/销售/金融/教育/制造等，不限行业）
- **核心动作**：用户输入 → 简历解析 → 多平台采集 → 简历驱动评分 → 排序 → 输出 HTML + Word + PDF
- **最小输入（3 项，缺一不可）**：
  1. **目标城市**（一个或多个，必填，如"杭州""杭州、厦门"）——城市是第一排序依据
  2. **目标岗位名称**（必填，如"Java后端开发" / "大模型评测验收专员"）
  3. **求职者简历文件**（PDF / Word / 文本 / 图片，必填，匹配度的唯一事实来源）
- **输出（3 份，一份不可少）**：`岗位筛选结果_<目标岗位>.html`、`岗位筛选结果_<目标岗位>.docx`、`岗位筛选结果_<目标岗位>.pdf`，三者内容一致，**先按城市分组、组内按匹配度从高到低排序**
- 可选补充参数（不强制）：期望薪资、学历偏好、行业偏好、排除条件——未提供时全部从简历推断，推断不出的标注"简历未体现"

## 执行流程（AI 必须按顺序执行）

```
确认输入(城市+岗位+简历) → 解析简历 → 构造检索关键词 → 多平台采集(每城市≥2平台) → 清洗去重 → 简历驱动评分 → 先城市后匹配度排序 → 生成HTML+Word+PDF三份交付物
```

### 第一步：确认输入并解析简历（匹配度的唯一依据）

1. **缺一不可的检查**：若用户未同时给出【城市】【目标岗位】【简历】，主动请求补齐，例如："请提供目标城市（可多个）、目标岗位名称，并发送简历文件。"
2. **读取简历**：
   - PDF → 用 pdf 技能解析提取文本
   - Word / txt / md / 图片 → 直接读取
3. **提取结构化信息**，每项注明来源（简历原文引用 或 "简历未填写"）：

| 字段 | 说明 | 提取规则 |
|------|------|----------|
| 姓名/求职意向 | 候选岗位方向 | 简历"求职意向"优先，无则用"目标岗位"字段 |
| 最高学历 | 大专/本科/硕士/博士 | 以简历"教育背景"最高学历为准 |
| 工作年限 | 累计相关经验（年） | 从简历"工作经历"累计计算 |
| 核心技能 | 简历明确列出的技能 | 逐条摘录，标注熟练程度（了解/熟练） |
| 项目经验 | 简历中的项目/成果 | 用于技能命中加分，备注须注明项目名 |
| 行业背景 | 过往所属行业 | 用于行业匹配 |
| 期望薪资 | 范围或最低值 | 简历有则用，无则"面议"（评分给10分） |

**规则**：
- 简历缺失字段一律标注"**简历未填写**"，禁止用合理猜测替代简历。
- 简历关键内容在表格备注中引用原文，保证可追溯。

### 第二步：检索关键词构造

- 主关键词 = 目标岗位名称（必选）
- 检索句式：`<岗位名称> 招聘 <城市>`、`<城市> <岗位名称>`
- 副关键词 = 简历中 3~5 个核心技能（用于匹配度评估与二次检索）
- 组合示例：`Java后端招聘 杭州`、`SpringBoot 招聘 杭州`、`上海 新媒体运营`

### 第三步：权威数据源（优先按此顺序采集）

| 优先级 | 平台 | 适用岗位 | 使用方式 |
|--------|------|----------|----------|
| 1 | BOSS 直聘 (zhipin.com) | 互联网/技术/市场运营 | websearch + webfetch 职位详情 |
| 2 | 智联招聘 (zhaopin.com) | 综合/白领/技术 | websearch + webfetch |
| 3 | 前程无忧 (51job.com) | 综合/传统行业 | websearch + webfetch |
| 4 | 猎聘 (liepin.com) | 中高端明星/管理岗 | websearch + webfetch |
| 5 | 国聘 (iguopin.com) | 央企/国企 | websearch + webfetch |
| 6 | 高校就业信息网/事业单位 | 应届生/编制岗 | websearch + webfetch |
| 7 | 目标公司官网招聘页 | 知名企业定向投递 | webfetch |

**采集规则**：
- 每个城市至少检索 2 个平台，每平台收集 5~10 个有效岗位
- 优先采集含薪资范围、学历要求、工作年限、技能清单的岗位
- 无法访问/反爬页面，改用 websearch 摘要中的职位描述字段
- 优先"靠谱"来源：官方发布、发布时间 30 天内、信息完整

### 第四步：岗位信息结构化

为每个岗位建立统一结构（字段不全则标注"未公开"）：

```
岗位名称 | 公司名称 | 地点(城市-区) | 薪资范围 | 学历要求 | 工作年限 | 技能要求 | 发布时间 | 来源平台 | 原始链接
```

### 第五步：基于简历进行匹配度评分（0~100 分）

**评分铁律：所有维度一律以简历提取出的真实信息为输入，禁止用问答猜测数据替代简历字段。**

| 评分维度 | 权重 | 评分标准（输入全部来自简历） |
|----------|------|----------|
| 技能匹配 | 40% | 命中简历核心技能数 / 岗位要求技能数 × 40。技能须**逐字出自简历技能清单**；"了解"计0.5命中，"熟练"计1命中；命中项在备注中引用简历原文 |
| 经验匹配 | 20% | 用简历累计工作年限：岗位年限要求在简历年限±1年内得满分，每偏差1年扣5分 |
| 学历匹配 | 15% | 以简历最高学历：岗位要求≤简历学历得满分；要求高于简历学历扣15分；简历未写学历给5分并标注 |
| 薪资匹配 | 15% | 简历期望薪资 vs 岗位薪资：完全覆盖15分、部分重合10分、低于期望5分；简历未写期望→给10分并标"面议" |
| 行业匹配 | 10% | 岗位行业与简历行业背景命中得满分、简历未体现行业得5分、明确冲突得0分 |

**简历驱动的特殊规则**：
- 简历"项目经验"命中岗位技能 → 提升技能匹配分（每个相关项目视为1项命中，注明项目名）。
- 简历无任何相关经验/技能 → 技能匹配分上限15分，标注"简历无相关经验，属跨行求职"。
- 简历缺失某字段 → 该维度按兜底分计，备注标注"**简历未填写**"。
- 匹配度与分项依据必须在"备注"列或表尾注明来源（哪一行简历内容得出），保证可复核。

评分建议阈值：
- **85-100 分**：高匹配，优先投递
- **70-84 分**：良好匹配，可投递
- **50-69 分**：一般匹配，视情况投递
- **<50 分**：不推荐（默认剔除，除非用户要求全部展示）

### 第六步：排序规则

1. **第一优先级（主排序）：城市**
   - 按用户给出的城市顺序分组，用户列出的第一个城市在前
2. **第二优先级（次排序）：匹配度（从高到低）**
   - 城市相同按匹配度降序
   - 匹配度相同按发布时间倒序（新职位在前）

> 即：**城市分组 → 组内匹配度由高到低**，这也是最终 HTML 的直接排版顺序。

### 第七步：生成三份交付文档（HTML + Word + PDF）

**输出文件**：保存到当前工作目录，三份并列：
- `岗位筛选结果_<目标岗位>.html`（网页版，可视化最强）
- `岗位筛选结果_<目标岗位>.docx`（Word 版，便于二次编辑/投递备份）
- `岗位筛选结果_<目标岗位>.pdf`（PDF 版，便于打印/正式发送）

**三份文档内容必须完全一致**：同一份岗位数据、同一个排序（先城市后匹配度）、同一匹配度评分、同一备注依据；仅排版载体不同。HTML 版在此实现，Word/PDF 与之数据联动。

**四种实现路径（按本机可用性择优）**：
1. **Pandoc 一键转换**（优先）：先写好 HTML，再用 `pandoc 岗位筛选结果_<目标岗位>.html -o 岗位筛选结果_<目标岗位>.docx` 和 `pandoc 岗位筛选结果_<目标岗位>.html -o 岗位筛选结果_<目标岗位>.pdf`（需 pandoc + LaTeX）。
2. **docx 库生成**：本机有 Python 时用 `python-docx` 直接写 Word 表格（标题/表格/备注各按 HTML 同样内容）。
3. **pdf 库生成**：用 `reportlab` 或 `fpdf2` 生成 PDF 表格；或用浏览器无头打印（Chrome/Edge headless `--print-to-pdf`）。
4. **Office COM 转换**：Windows 平台且装有 Microsoft Word 时，用 PowerShell `New-Object -ComObject Word.Application` 打开 html→另存为 docx 和 pdf。
- **回退兜底**：若本机无任何转换工具，允许生成纯文本 `_<目标岗位>.txt` 作为 Word 替代，但必须向用户说明并请其安装 Pandoc；不许以"生成不了"结束任务。

**表格结构**（HTML 版）：

```
编号 | 岗位名称 公司 | 地点 | 薪资 | 学历 | 经验 | 匹配度(评分+进度条) | 来源 | 链接 | 备注
```

**HTML 规范要求**：
- 使用 UTF-8 编码，`<meta charset="UTF-8">`
- 标题：`岗位筛选结果 - {目标岗位}`
- 顶部信息栏：目标岗位、目标城市（用户输入）、**评分依据（简历最高学历/工作年限/核心技能，逐项注明"简历原文"）**、评分时间
- 表头 `<thead>`，数据行 `<tbody>`
- 匹配度列：分数 + 彩色进度条（>85 绿 / 70-84 蓝 / 50-69 橙 / <50 红）
- 链接列：`<a href="{链接}" target="_blank">查看</a>`
- 按城市分组添加 `<h2>` 城市小标题，组内按匹配度降序（城市顺序 = 用户输入顺序）
- 内嵌 CSS 美化：斑马纹行、hover 高亮、居中排版、响应式容器
- 备注列体现**简历驱动的评分依据**（命中技能来自简历"技能清单"第X行；学历/年限取自"教育背景/工作经历"）

**Word 文档（docx）要求**：
- 标题：`岗位筛选结果 - {目标岗位}`；副标题：求职者、目标城市、评分时间
- 用 Word 表格呈现岗位数据，列同 HTML（编号/岗位公司/地点/薪资/学历/经验/匹配度/来源/链接/备注）
- 按城市分组用标题段落分隔，组内按匹配度降序
- 设置合理行高列宽、表头粗体、加边框，保证打印可读

**PDF 文档（pdf）要求**：
- A4 纵向，页边距适中，中文字体嵌入（宋体/黑体等系统字体即可）
- 标题、城市分组、岗位表格、匹配度、备注逐项呈现，跨页自动分页
- 内容与 HTML/Word 完全一致，文件名相同仅后缀不同

## 输出示例（供参考）

```
# 岗位筛选结果 - Java 后端开发
求职者：<简历姓名> | 目标城市：北京、杭州 | 评分时间：2026-08-01

## 北京（用户第1个城市）
| 编号 | 岗位 | 公司 | 地点 | 薪资 | 匹配度 |
| 1  | Java开发工程师 | 某大厂   | 北京-海淀 | 25-40K | 92% |
| 2  | 后端开发工程师 | 某中厂   | 北京-朝阳 | 20-35K | 85% |

## 杭州（用户第2个城市）
| 编号 | 岗位 | 公司 | 地点 | 薪资 | 匹配度 |
| 3  | Java后端       | 某独角兽 | 杭州-西湖 | 22-35K | 88% |
| 4  | 后端工程师     | 某创业公司 | 杭州-滨江 | 15-22K | 72% |
```

## 注意事项
- 所有岗位信息必须标注来源平台和链接，禁止虚构岗位
- 匹配度必须以简历文件提取的真实信息为唯一评分依据，禁止用问答占位数据替代简历字段
- 简历缺失字段标注"简历未填写"，不编造；检索不到的信息标注"面议"/"未公开"
- 若某城市无匹配岗位，如实说明"该城市暂无符合要求的岗位"
- 最终必须生成三份文件（HTML、Word、PDF）并在回复中告知三个文件路径；三者内容须一致，禁止只出 HTML 就了事
- 生成 Word/PDF 时先探测工具可用性（pandoc / python-docx / reportlab / Word COM），选可用路径，回退方案需向用户说明
- 完成后向用户简要总结：共筛选 N 岗位、各城市数量、最推荐前 3 个岗位及推荐依据（来自简历哪部分内容），并列出三个输出文件路径